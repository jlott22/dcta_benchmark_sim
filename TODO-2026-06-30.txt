DCTA Benchmark Simulation TODO
Date: 2026-06-30
Updated: 2026-07-03

Status key
----------
[VALID]          Existing result is still valid under the current algorithm definitions.
[RERUN NEEDED]   Existing result is stale because algorithm definitions changed.
[MADE - NOT RUN] Test implementation and scripts exist, but the experiment has not been run.
[RUNNING]        Experiment is currently running with live CSV output.
[QUEUED]         Experiment is scripted and queued behind the current running stage.
[PARTIAL]        Some current results exist, but the experiment is not complete.
[NOT MADE]       Test implementation and scripts still need to be created.
[NOT CURRENT]    Not part of the current rerun/tuning plan.


Current Valid Result
--------------------

1. [VALID] DGA iteration sensitivity for clue-informed search
   Keep DGA_ITERATIONS_PER_TRIGGER = 25 from the prior clue-informed DGA
   iteration sensitivity results. This is the only existing sensitivity result
   currently treated as valid.


Current Rerun Plan
------------------

2. [PARTIAL] Per-algorithm horizon tuning for clue-informed search
   Current horizon sensitivity for ACBBA, PI, HIPC, DMCHBA, and DGA uses ideal
   communication and Bernoulli p=0.25 only. Top-K tuning is not included.
   Imported partial/complete outputs are in
   clue_sensitivity_test_results/horizon_results/raw and combined summaries are
   in clue_sensitivity_test_results/horizon_results/combined.

3. [RERUN NEEDED] Final clue-informed benchmark
   Rerun the full clue-informed benchmark for all retained algorithms after
   horizon choices are locked. Use one frozen commit and one generated manifest.
   Existing clue_500_combined results are stale/exploratory because algorithm
   behavior and fairness controls have changed.


Grid-Size / Robot-Density Tests
-------------------------------

4. [RERUN NEEDED] Coverage environment
   Rerun grid-size / robot-density sensitivity under the current algorithm
   definitions.

5. [RERUN NEEDED] Clue-informed search environment
   Rerun grid-size / robot-density sensitivity under the current algorithm
   definitions.

6. [RERUN NEEDED] Known-target environment
   Rerun grid-size / robot-density sensitivity under the current algorithm
   definitions.


Core Benchmark Tests
--------------------

7. [RERUN NEEDED] Core clue-informed search environment
   Rerun all retained algorithms after horizon tuning is complete.

8. [RERUN NEEDED] Core coverage environment
   Rerun coverage core trials under the current algorithm definitions.

9. [RERUN NEEDED] Core known-target environment
   Rerun the known-target core benchmark with 500 trials for each required
   algorithm and condition.


Horizon Sensitivity Tests
-------------------------

10. [RERUN NEEDED] Clue-informed search environment
    Existing clue-informed horizon sensitivity is stale because algorithm
    definitions changed. Replacement run is currently in progress under item 2.
    Old results are retained only for sanity checks; they do not use the same
    scenario geometry as the current run, even where trial_id values overlap.

11. [RERUN NEEDED] Known-target environment
    Existing known-target horizon sensitivity is stale under the current
    algorithm definitions.

12. [QUEUED] Coverage environment
    Coverage horizon sensitivity is scripted in
    benchmark_sim/tests/run_current_sensitivity_reruns.py and queued after the
    clue-informed horizon stage and known-target DGA iteration stage. It is set
    to run 50 trials per condition with live CSV output.


Top-K Sensitivity Tests
-----------------------

13. [NOT CURRENT] Clue-informed search environment
    Top-K sensitivity is not part of current tuning and has no bearing on the
    current horizon/DGA-iteration decisions.

14. [NOT CURRENT] Known-target environment
    Do not create or run Top-K candidate sensitivity testing for the current
    rerun plan.


DGA Iteration Sensitivity Tests
-------------------------------

15. [VALID] Clue-informed search environment
    Prior clue-informed DGA iteration sensitivity remains valid for selecting
    25 iterations per trigger.

16. [RERUN NEEDED] Known-target environment
    Known-target DGA iteration sensitivity is queued after the current
    clue-informed horizon run. It will run ideal and Bernoulli p=0.25 at
    iteration settings 1, 2, 5, 10, 25, and 50 using live CSV output and resume
    support.


Current Run Notes
-----------------

17. [PARTIAL] Current sensitivity rerun orchestrator
    Script: benchmark_sim/tests/run_current_sensitivity_reruns.py
    Current stage order:
    1. clue-informed horizon sensitivity, 300 trials per condition
    2. known-target DGA iteration sensitivity, 300 trials per condition
    3. coverage horizon sensitivity, 50 trials per condition

    The runner now writes live CSVs after each trial, resumes from existing CSV
    trial IDs, and marks individual failed trials without stopping the whole
    condition. The orchestrator uses a shared work queue and can adopt already
    running condition processes during restart. Resume by running:
    python -u -m benchmark_sim.tests.run_current_sensitivity_reruns

18. [PARTIAL] Comparison against old horizon data
    The archived stale clue horizon data at:
    C:\Users\lottj\Desktop\research\decentralized benchmark\old results decentralized benchmark\2026-07-03_stale_after_algorithm_changes\clue_sensitivity_test_results\horizon_results
    has matching condition names and trial_id ranges, but the actual target and
    clue geometries differ from the current run. Do not use old-vs-current as a
    paired trial comparison; use it only as a rough sanity check.
